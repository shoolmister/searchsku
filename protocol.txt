i need to create a simple search engine webpage on local pc for my products which when i insert an SKU it will retrieve results from my database. i want the database to be implemented in the source itself, and when i want to update i just need to past the new data information in the relevant section in the script source. the database including SKU, DESCRIPTION, ETA and ATA. is it be smart to use xml system for it?